<h1>Wiki</h1>
  <%= link_to "Back to Wikis", wikis_path %>

  <% if policy(Wiki.new).show? %>
    <h1><%= markdown_to_html @wiki.title %></h1>
    <p><%= markdown_to_html @wiki.body %></p>
    <small>
  	  <i> Written by: <%= @wiki.user.email %></i>
    </small>
  <% end %> 

    <br> 
		  
  <% if policy(Wiki.new).update? %>
    <%= link_to "Edit", edit_wiki_path(@wiki.id), class: 'btn btn-success' %>
    <%= link_to "Delete Wiki", [@wiki], method: :delete, class: 'btn btn-danger', data: {confirm: 'Are you sure you want to delete this wiki?'} %>
  <% end %>